{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Post": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "updatedAt"
      ]
    }
  },
  "type": "object",
  "properties": {
    "post": {
      "$ref": "#/definitions/Post"
    }
  }
}